# Flutter and Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub/
build/

# Generated files (Hive / build_runner)
**/*.g.dart
**/*.freezed.dart

# Android/Gradle
.gradle/
android/.gradle/
android/local.properties
android/captures/
android/key.properties
**/*.jks
**/*.keystore

# IDE / editor
.idea/
*.iml
*.iws
*.ipr
.DS_Store
*.swp
*.swo
*~
.vscode/settings.json

# Logs & analysis output
*.log
logs/
assets/data/analyze.log

# Environment / secrets
.env
.env.local
.env.production
supabase/.env
android/app/client_secret_*.json
google-services.json

# Node (Firebase CLI / tooling only — not app code)
node_modules/
package-lock.json

# Distribution / build scripts output
scripts/build-output/

# LaTeX build artifacts (pdfs/*.tex and *.pdf are fine, these are not)
*.aux
*.fls
*.fdb_latexmk
*.synctex.gz
pdfs/*.out

# Supabase CLI cache (machine-local)
supabase/.temp/

# Build logs (never commit)
build_log*.txt
build_log*.log

# Duplicate assets folder guard
lib/assets/

# Dart generated files (hive, freezed, build_runner)
**/*.g.dart
**/*.freezed.dart
